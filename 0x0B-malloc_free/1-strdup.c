#include "main.h"
#include <stdio.h>

/**
 * *_strdup - entry
 * @str: string
 * Return: char
 */

char *_strdup(char *str)
{
	char *array = (char*)malloc (20 * sizeof(char))
	for (int i = 0; i < 20; i++)
	{
		str = arr[i];		
	}
	return (str);
}
